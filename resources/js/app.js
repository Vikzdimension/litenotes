import "./bootstrap";
import Alpine from "alpinejs";
import sNoteImage from "../logo/s_note.png"; // Import image

window.Alpine = Alpine;
Alpine.start();

// Example of using the image
// const img = document.createElement("img");
// img.src = sNoteImage; // Use the imported image URL
// img.alt = "S Note Image";
// document.body.appendChild(img);
